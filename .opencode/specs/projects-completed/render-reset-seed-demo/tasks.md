# Tareas

- [x] Revisar scripts de Render, `render.yaml`, configuración SQLite, `routes/console.php`, seeder y README.
- [x] Proteger el release con las condiciones explícitas de demo/SQLite y ruta SQLite esperada.
- [x] Sustituir migración + seed condicional por `migrate:fresh --seed --force` como flujo único.
- [x] Mantener la preparación `mkdir`/`touch` sin presentarla como persistencia.
- [x] Documentar release destructivo, filesystem efímero y diferencia entre release y restart.
- [ ] Ejecutar validación PHP/Laravel (no permitida porque requiere `php.exe`).
- [ ] Ejecutar revisión posterior por `reviewer-agent`.
- [ ] Confirmar aceptación funcional en Render.
