# Tasks

1. Locate the ErrorBoundary fallback and all existing SweetAlert usage.
2. Remove the misleading fallback view and prevent duplicate/unmounted DOM handling.
3. Route expected API/catalog/mutation errors through the existing SweetAlert convention.
4. Verify no remaining references render “No se pudo mostrar esta sección”.
5. Run lint, build and targeted frontend checks.
