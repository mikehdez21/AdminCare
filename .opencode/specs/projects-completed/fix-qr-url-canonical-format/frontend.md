# Frontend

No se requieren cambios de React: la ruta `/activosfijos/qraf/:codigoQR` ya
existe y consume el resolver API. El backend debe producir URLs con ese mismo
path y con el dominio configurado por `FRONTEND_URL`.
