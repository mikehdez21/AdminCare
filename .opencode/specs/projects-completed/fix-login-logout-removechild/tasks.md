# Tasks

1. Reproduce/trace the blank login/logout transition and identify the DOM owner conflict.
2. Inspect auth restore, logout, interceptor and router/Suspense lifecycle.
3. Implement the smallest frontend/auth fix without direct DOM removal or server restarts.
4. Verify login, session expiration and logout using already-running local servers.
5. Run lint/build/type checks and review the diff.
