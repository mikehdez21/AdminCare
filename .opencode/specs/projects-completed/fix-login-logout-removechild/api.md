# API

Inspect existing `/sanctum/csrf-cookie`, `/auth/check`, `/auth/login` and `/auth/logout` contracts. Do not change backend routes unless the investigation proves a session/cookie contract defect. Preserve `{success,message,...data}` responses and Sanctum cookie requirements.
