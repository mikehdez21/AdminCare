# API

Modify only the implementation behind the existing individual fixed-asset movements endpoint. Preserve its method, route, request filters, pagination and `{ success, message, ...data }` response shape. Preserve required RBAC permissions.
