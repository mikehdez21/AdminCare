# Tasks

1. Locate the original SQL view, endpoint, model/service and frontend consumer.
2. Compare required view columns with SQLite tables and demo seed data.
3. Implement a portable SQLite path and seed representative synthetic movement rows.
4. Verify endpoint filters, pagination, permissions and response shape.
5. Run SQLite fresh seed, endpoint smoke test, PHP lint and frontend checks if touched.
