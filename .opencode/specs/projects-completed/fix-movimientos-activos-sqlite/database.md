# Database

Inspect the original movement view and the SQLite demo schema/seed. Add only the missing SQLite columns, indexes, joins or synthetic seed records required by the endpoint. Do not add a database view if a query-builder/table path is portable and sufficient.
