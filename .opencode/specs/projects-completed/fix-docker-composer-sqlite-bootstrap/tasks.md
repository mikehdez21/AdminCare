# Tareas

- [x] Inspeccionar Dockerfile, etapas frontend/PHP y scripts de Render.
- [x] Mover la creación de SQLite antes de `composer install`.
- [x] Quitar el `touch` posterior duplicado y conservar la verificación `pdo_sqlite`.
- [x] Ejecutar `git diff --check` y revisar el diff.
- [ ] Ejecutar una validación Docker estática; bloqueada porque Docker no está instalado/disponible en el entorno.
