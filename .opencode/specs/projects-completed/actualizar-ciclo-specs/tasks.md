# Tasks

1. Crear esta especificación en `active-projects` antes de modificar la
   documentación o mover carpetas.
2. Actualizar `AGENTS.md` con la regla explícita de archivo posterior a la
   validación del usuario.
3. Reforzar la regla equivalente en `.opencode/agents/orchestrator.md`.
4. Crear `projects-completed` y mover allí las 8 carpetas existentes,
   conservando todos sus archivos.
5. Mover también `actualizar-ciclo-specs` al destino.
6. Verificar que `active-projects` esté vacío, que el destino tenga 9 carpetas
   y ejecutar `git diff --check`.
