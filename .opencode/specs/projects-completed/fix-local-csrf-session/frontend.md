# Frontend

Configure the API client for local cross-origin credentials, CSRF-cookie acquisition, XSRF header forwarding and renewal after 419 responses. Login/logout must not silently continue when CSRF acquisition fails.
