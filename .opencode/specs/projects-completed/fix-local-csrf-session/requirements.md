# Requirements

Fix the local Sanctum CSRF flow for the development topology:

- Laravel: `http://127.0.0.1:8000`
- React/Vite: `http://localhost:5173`

Login, session check and logout must work locally without `CSRF token mismatch`. Do not change Render deployment configuration in this task.
