# API

Reuse existing fixed-asset detail API and permissions. Add no QR-storage endpoint. If a public scan resolver is required, document its route and ensure it exposes only the intended asset page behavior, not sensitive data.
