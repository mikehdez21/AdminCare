# Database

No schema or seed changes are expected. The QR payload uses the existing fixed-asset identifier/code. Confirm that the asset detail lookup works from the identifier and that no QR upload/storage is required.
