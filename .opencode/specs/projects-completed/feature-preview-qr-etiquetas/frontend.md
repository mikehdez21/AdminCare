# Frontend

- Update the labels/QR preview components under `frontend/src/js/components/`.
- Generate QR in memory/browser only.
- Encode the canonical asset detail URL.
- Provide loading/empty/error handling when no asset is selected.
- Ensure scanning/navigation works on the deployed base URL and local development URL.
